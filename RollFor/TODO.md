# TODO

1. Fix SR info in the LootFrame if 2 items dropped and 2 different players soft res it (works currently in DroppedLootAnnounce - replicate that).
2. Disable the loot frame when rolling.
3. Make loot info messages less verbose (don't show looted items below threshold).
4. LootAwardPopup doesn't center on RollingPopup.
5. Deal with multiple selected items (if many of the same item dropped).
6. Deal with /rf 2x (the second winner shows up in the popup).

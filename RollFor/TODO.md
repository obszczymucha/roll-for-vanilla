# TODO

1. Backport LibDeflate.
2. Fix a bug where SR item gets raid rolled if no one rolls.
3. RollingPopup - rolls don't align: 41, 24, 23.
5. Add class colors to player names in messages.
6. Replace colorcodes with functions.
7. 2 rolls become 1 after rolling. Reproduce: /srs then roll then /srs again.
8. Add raid roll again as a config option disabled by default.
9. Add auto raid-roll enabled.
